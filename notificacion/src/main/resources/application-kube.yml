server:
  port: 8082
spring:
  application:
    name: notificacion
  config:
    import: "optional:configserver:http://config-server:8090"
    cloud:
      config:
        enabled: true
        uri: http://config-server:8090